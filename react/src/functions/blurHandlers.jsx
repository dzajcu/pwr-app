export const emailInputBlurHandler = (e, setEmailTouched) => {
	setEmailTouched(true)
}

export const usernameInputBlurHandler = (e, setUsernameTouched) => {
	setUsernameTouched(true)
}

export const passwordInputBlurHandler = (e, setPasswordTouched) => {
	setPasswordTouched(true)
}

export const passwordValidatedInputBlurHandler = (e, setPasswordValidatedTouched) => {
	setPasswordValidatedTouched(true)
}
