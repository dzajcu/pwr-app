export const clearInputsRegister = (setEmail, setUsername, setPassword, setPasswordValidated) => {
	setEmail('')
	setUsername('')
	setPassword('')
	setPasswordValidated('')
}

export const clearInputsLogin = (setLogin, setPassword) => {
	setLogin('')
	setPassword('')
}
